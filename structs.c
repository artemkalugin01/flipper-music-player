#include "structs.h" // Include its own header to ensure consistency 


const char *menu_items[] = {
    "Browse",
    "Shuffle",
    "About",
};

const int menu_items_count = sizeof(menu_items) / sizeof(char *);
